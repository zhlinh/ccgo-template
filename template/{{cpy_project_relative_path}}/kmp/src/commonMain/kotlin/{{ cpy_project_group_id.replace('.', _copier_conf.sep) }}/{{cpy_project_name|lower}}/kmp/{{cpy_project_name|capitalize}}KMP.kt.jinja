package {{cpy_project_group_id}}.{{cpy_project_name|lower}}.kmp

/**
 * {{cpy_project_name|capitalize}} Kotlin Multiplatform API
 *
 * This is the main entry point for the {{cpy_project_name|capitalize}} library in Kotlin Multiplatform projects.
 * It provides a cross-platform API that wraps the native C++ implementation.
 *
 * Usage example:
 * ```kotlin
 * val {{cpy_project_name|lower}} = {{cpy_project_name|capitalize}}KMP()
 * {{cpy_project_name|lower}}.setDebugLog(true)
 * val version = {{cpy_project_name|lower}}.getVersion()
 * ```
 */
expect class {{cpy_project_name|capitalize}}KMP() {
    /**
     * Enable or disable debug logging
     *
     * @param enable true to enable debug logging, false to disable
     */
    fun setDebugLog(enable: Boolean)

    /**
     * Get the library version string
     *
     * @return Version string (e.g., "1.0.0")
     */
    fun getVersion(): String

    /**
     * Get the platform name
     *
     * @return Platform name (e.g., "Android", "iOS", "Desktop")
     */
    fun getPlatformName(): String
}

/**
 * Global instance factory for {{cpy_project_name|capitalize}}KMP
 *
 * This provides a convenient way to access the library without managing instances manually.
 *
 * Usage:
 * ```kotlin
 * {{cpy_project_name|capitalize}}KMP.getInstance().setDebugLog(true)
 * ```
 */
object {{cpy_project_name|capitalize}}KMPFactory {
    private var instance: {{cpy_project_name|capitalize}}KMP? = null

    /**
     * Get or create the singleton instance
     */
    fun getInstance(): {{cpy_project_name|capitalize}}KMP {
        return instance ?: {{cpy_project_name|capitalize}}KMP().also { instance = it }
    }

    /**
     * Reset the singleton instance (useful for testing)
     */
    fun reset() {
        instance = null
    }
}
