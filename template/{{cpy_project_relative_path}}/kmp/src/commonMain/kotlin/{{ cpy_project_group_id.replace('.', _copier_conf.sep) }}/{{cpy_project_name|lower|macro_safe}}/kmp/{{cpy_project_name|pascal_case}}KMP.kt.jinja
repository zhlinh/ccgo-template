package {{cpy_project_group_id}}.{{cpy_project_name|lower|macro_safe}}.kmp

/**
 * {{cpy_project_name|pascal_case}} Kotlin Multiplatform API
 *
 * This is the main entry point for the {{cpy_project_name|pascal_case}} library in Kotlin Multiplatform projects.
 * It provides a cross-platform API that wraps the native C++ implementation.
 *
 * Usage example:
 * ```kotlin
 * val {{cpy_project_name|lower|macro_safe}} = {{cpy_project_name|pascal_case}}KMP()
 * {{cpy_project_name|lower|macro_safe}}.setDebugLog(true)
 * val version = {{cpy_project_name|lower|macro_safe}}.getVersion()
 * ```
 */
expect class {{cpy_project_name|pascal_case}}KMP() {
    /**
     * Enable or disable debug logging
     *
     * @param enable true to enable debug logging, false to disable
     */
    fun setDebugLog(enable: Boolean)

    /**
     * Get the library version string
     *
     * @return Version string (e.g., "1.0.0")
     */
    fun getVersion(): String

    /**
     * Get the platform name
     *
     * @return Platform name (e.g., "Android", "iOS", "Desktop")
     */
    fun getPlatformName(): String
}

/**
 * Global instance factory for {{cpy_project_name|pascal_case}}KMP
 *
 * This provides a convenient way to access the library without managing instances manually.
 *
 * Usage:
 * ```kotlin
 * {{cpy_project_name|pascal_case}}KMP.getInstance().setDebugLog(true)
 * ```
 */
object {{cpy_project_name|pascal_case}}KMPFactory {
    private var instance: {{cpy_project_name|pascal_case}}KMP? = null

    /**
     * Get or create the singleton instance
     */
    fun getInstance(): {{cpy_project_name|pascal_case}}KMP {
        return instance ?: {{cpy_project_name|pascal_case}}KMP().also { instance = it }
    }

    /**
     * Reset the singleton instance (useful for testing)
     */
    fun reset() {
        instance = null
    }
}
