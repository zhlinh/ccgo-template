//
// global_var.h
// base
//
// Created by ccgo on {{ '%Y-%m-%d' | strftime }}.
// Copyright {{ '%Y' | strftime }} ccgo Project Authors. All rights reserved.

#ifndef {{ cpy_project_name|upper }}_BASE_GLOBAL_VAR_H_
#define {{ cpy_project_name|upper }}_BASE_GLOBAL_VAR_H_

#include <map>
#include <set>
#include <sstream>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>

#ifdef __ANDROID__
#  include <jni.h>
#endif

namespace {{ cpy_project_name|lower }} {

#define {{ cpy_project_name|upper }}_GV ({{ cpy_project_name|lower }}::GlobalVar::GetInstance())
#define {{ cpy_project_name|upper }}_DEBUG ({{ cpy_project_name|lower }}::GlobalVar::GetInstance().debug_)

class GlobalVar {
 private:
  GlobalVar() = default;
  ~GlobalVar() = default;
  GlobalVar(const GlobalVar &) = delete;
  GlobalVar &operator=(const GlobalVar &) = delete;

 public:
  static GlobalVar &GetInstance();

  void Clear();

  bool debug_ = false;
};

}  // namespace {{ cpy_project_name|lower }}

#endif  // {{ cpy_project_name|upper }}_BASE_GLOBAL_VAR_H_
